const redLeaveScore = ref(database, "redLeaveScore");
set(redLeaveScore, 0);
//counts the number of scraps in auto for RP, not a point value
const redAutoScraps = ref(database, "redAutoScraps");
set(redAutoScraps, 0);
const redHighBinScrapScore = ref(database, "redHighBinScrapScore");
set(redHighBinScrapScore, 0);
const redLowBinScrapScore = ref(database, "redLowBinScrapScore");
set(redLowBinScrapScore, 0);
const redVaultScrapScore = ref(database, "redVaultScrapScore");
set(redVaultScrapScore, 0);
const redCoreScore = ref(database, "redCoreScore");
set(redCoreScore, 0);
const redParkScore = ref(database, "redParkScore");
set(redParkScore, 0);
const redFouls = ref(database, "redFouls");
set(redFouls, 0);

const blueLeaveScore = ref(database, "blueLeaveScore");
set(blueLeaveScore, 0);
//counts the number of scraps in auto for RP, not a point value
const blueAutoScraps = ref(database, "blueAutoScore");
set(blueAutoScraps, 0);
const blueHighBinScrapScore = ref(database, "blueHighBinScrapScore");
set(blueHighBinScrapScore, 0);
const blueLowBinScrapScore = ref(database, "blueLowBinScrapScore");
set(blueLowBinScrapScore, 0);
const blueVaultScrapScore = ref(database, "blueVaultScore");
set(blueVaultScrapScore, 0);
const blueCoreScore = ref(database, "blueCoreScore");
set(blueCoreScore, 0);
const blueParkScore = ref(database, "blueParkScore");
set(blueParkScore, 0);
const blueFouls = ref(database, "blueFouls");
set(blueFouls, 0);